import studentData from "./data.json"
import cohortListData from "./CohortsData"

// input: studentData
// output: array of arrays where each nested array is list of students in specific cohort
function getArraysFromStudentData(students) {

}